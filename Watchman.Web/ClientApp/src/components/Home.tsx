import * as React from 'react';
import { connect } from 'react-redux';

const Home = () => (
   <div>
        <h2>Welcome in Watchman management system!</h2>
        v2
  </div>
);

export default connect()(Home);
