using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;
using Devscord.DiscordFramework.Middlewares.Contexts;

namespace Devscord.DiscordFramework.Services
{
    public class ChannelsService
    {
        public Task SetPermission(ChannelContext channelContext, Permission permission)
        {
            
        }

        public Task SetPermissions(ChannelContext channelContext, Permissions permissions)
        {

        }
    }
}
